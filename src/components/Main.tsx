import React, { useState } from 'react';
import Counter from './Counter';

export default () => {

    return (
        <>
            <Counter />
            <Counter />
            <Counter />
            <Counter />
        </>
    );
}